
    <tr>
      <td><%= employee.id %></td>
      <td><%= employee.name %></td>
      <td><%= employee.designation %></td>
      <td><%= employee.salary %></td>
      <td><%= employee.manager&.name || '-' %></td>
      <td> <%= link_to 'Destroy Employee', [employee.department, employee], data: { turbo_method: :delete, turbo_confirm: "Are you sure?" } %> </td>
    </tr>
  
